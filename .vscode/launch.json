{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Application (macOS)",
            "type": "node-terminal",
            "request": "launch",
            "command": "pnpm run:macos"
        }
    ]
}
